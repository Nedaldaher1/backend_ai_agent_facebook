# agent/vision

Customer-image attribute extraction: given an image a customer sends, infer
attributes (color, fabric, occasion, …) and feed them into the product search
tool so the agent can match by photo.

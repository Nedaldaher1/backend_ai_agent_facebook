import { Injectable } from '@nestjs/common';

/**
 * COD order-draft capture and lifecycle. Exposed to the agent module.
 * TODO: inject OrdersRepository and implement once the orders/order_items
 * schema lands.
 */
@Injectable()
export class OrdersService {}
